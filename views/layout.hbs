<!DOCTYPE html>
<html>
  <head>
    {{> head }}
  </head>
  <body>
    <div id="container">
      <h1 id="site-header"><a href="/">Patrick Weaver: Blog</a></h1>
      {{# if index }}
        {{> pagination }}
      {{/ if }}

      {{> bars }}
      {{{ body }}}
      {{> bars }}
      {{#if index}}
        {{> pagination }}
      {{/if }}
      {{> script }}
      <div id="hide">
        <span id="apiUrl">{{ apiUrl }}</span>
      </div>
    </div>
  </body>
</html>
