<ul id="posts">
  {{#each posts}}
    {{> postPreview this}}
    {{#unless @last}}
      <hr style="border-top: 2px dotted {{ @root.hrBorderColors.a }}; border-bottom: 2px dotted {{ @root.hrBorderColors.b }};">
    {{/unless}}
  {{/each}}
</ul>
